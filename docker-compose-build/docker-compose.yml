version: '3'
services:
  service1:
    build:
      context: service1
      dockerfile: dockerfile1
      args:
        - APP_NAME=service1 
        - ENV=dev
        - BASE_IMAGE=node:16

  service2: 
    build:
      context: .
      dockerfile: dockerfile2
      args:
        - APP_NAME=service2
        - ENV=dev2
        - BASE_IMAGE=node:20